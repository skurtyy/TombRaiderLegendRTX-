name: Claude PR Review

on:
  pull_request:
    types: [labeled]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  call:
    if: github.event.label.name == 'claude-review'
    uses: ./.github/workflows/_claude-pr-review.yml
    secrets: inherit
