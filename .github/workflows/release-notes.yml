name: Release Notes on Tag

on:
  push:
    tags: ['v*']

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  call:
    uses: ./.github/workflows/_claude-release-notes.yml
    secrets: inherit
    with:
      attach_artifact_pattern: ''
