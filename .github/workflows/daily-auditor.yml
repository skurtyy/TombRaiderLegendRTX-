name: Daily Auditor

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  call:
    uses: ./.github/workflows/_daily-auditor.yml
    secrets: inherit
