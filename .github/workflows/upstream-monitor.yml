name: Upstream Monitor

on:
  schedule:
    - cron: '0 6 * * 2'
    - cron: '0 6 * * 5'
  workflow_dispatch:

jobs:
  call:
    if: vars.LINEAR_ENABLED == 'true'
    uses: ./.github/workflows/_upstream-monitor.yml
    secrets: inherit
